<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplicationlab6" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>
    <style name="Theme.Messenger" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="backgroundPrimaryColor">@color/black</item>
        <item name="backgroundSecondaryColor">@color/black</item>
        <item name="textColorPrimary">@color/black</item>
        <item name="textColorSecondary">@color/white</item>
        <item name="android:statusBarColor">?backgroundPrimaryColor</item>
        <item name="iconColorPrimary">@color/white</item>
        <item name="iconColorSecondary">@color/white</item>
        <!-- Цвет фона -->
        <item name="colorBackground">#1F1F1F</item>
        <!--Цвет заголовков-->
        <item name="titleTextColor">#D1D1D1</item>
        <!-- Цвет текста на кнопках -->
        <item name="buttonTextColor">#050505</item>
        <!-- Цвет фона кнопок -->
        <item name="buttonBackgroundColor">#FFFFFF</item>
    </style>

</resources>