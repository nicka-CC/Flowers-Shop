<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplicationlab6" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Messenger" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="backgroundPrimaryColor">@color/blue</item>
        <item name="backgroundSecondaryColor">@color/white</item>
        <item name="textColorPrimary">@color/black</item>
        <item name="textColorSecondary">@color/black</item>
        <item name="android:statusBarColor">?backgroundPrimaryColor</item>
        <item name="iconColorPrimary">@color/black</item>
        <item name="iconColorSecondary">@color/text_gray</item>
        <!-- Цвет фона -->
        <item name="colorBackground">#E0DBDB</item>
        <!-- Цвет заголовков -->
        <item name="titleTextColor">#313131</item>
        <!-- Цвет текста на кнопках -->
        <item name="buttonTextColor">#979797</item>
        <!-- Цвет фона кнопок -->
        <item name="buttonBackgroundColor">#FFFFFF</item>

    </style>

    <style name="Theme.MyApplicationlab6" parent="Base.Theme.MyApplicationlab6" />
</resources>